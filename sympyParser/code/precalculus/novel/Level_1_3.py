from sympy import symbols, pi, atan, sqrt

# Define the variable
x = sqrt(3)

# Compute arctan(sqrt(3))
theta = atan(x)

# Return the final answer in radians
final_answer = theta.evalf()
final_answer
